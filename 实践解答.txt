1. 直接使用SEQ-ACK机制即可.
发送方发送一个包，如果接收不到接收方回应，就重新发送。

2. 因为有随机的时延,且有可能丢包，所以该网络会有以下异常情况：
  - 数据乱序到达
  - 数据缺失
简单的实现：
接收方丢弃那些乱序到达的数据。发送方排序发送，等待ACK超时重发

传送速度尽可能大：使用到快速重传。

3. 需要用到:
需要使用windowSize机制来保证不被重复发送。

